import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'atividade_rec',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
